# Historical design snapshots

`0.1/` preserves the original MVP documents and the pre-implementation BCL/runtime plan byte-for-byte. Their status statements, counts, and relative links describe that historical version; they are not the current compatibility contract. Current documents live directly under `docs/`, and the implementation ledger is `docs/compatibility.md`.
